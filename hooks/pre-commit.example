#!/bin/sh
# Inactive example for the planned CLI. Do not install until the CLI and fixtures work.
set -eu

if ! command -v smells >/dev/null 2>&1; then
    printf '%s\n' 'smells: required scanner executable is unavailable' >&2
    exit 2
fi

exec smells check --staged --policy quality-policy.json
